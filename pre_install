#!/usr/bin/with-contenv bash

# Update Docker
sudo apt-get update
sudo apt-get upgrade docker-ce -y

# Set experiemental features on
mkdir -p ~/.docker/
echo '{"experimental": "enabled"}' > ~/.docker/config.json
