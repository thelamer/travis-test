sudo: true

language: bash

services:
  - docker

env:
  - DEBIAN_FRONTEND=noninteractive

jobs:
  include:
    - stage: Check
      script:
        - ./thelamer/travis-test/pre_install
        - docker --version
        - docker manifest
